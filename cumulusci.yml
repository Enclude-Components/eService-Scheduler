minimum_cumulusci_version: '4.6.0'
project:
    name: eService-Scheduler
    package:
        name: eService-Scheduler
        api_version: '63.0'
    dependencies:
        - github: 'https://github.com/EncludeLtd/eCASSV2.5'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75

flows:
  config_dev:
    steps:
      3:
        task: assign_permission_sets
        options:
          api_names: "eService_Scheduler_Admin"